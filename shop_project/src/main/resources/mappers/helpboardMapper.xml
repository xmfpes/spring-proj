<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	

	
<mapper namespace="org.zerock.mapper.HelpBoardMapper">
	<insert id="create">
		insert into tbl_help_board(title, content, writer) values(#{title}, #{content}, #{writer})
	</insert>
	<select id="read" resultType="org.zerock.domain.BoardVO">
		select bno, title, content, writer, regdate, viewcnt from tbl_help_board where bno = #{bno}
	</select>
	<update id="update">
		update tbl_help_board set title=#{title}, content=#{content}, writer=#{writer} where bno = #{bno}
	</update>
	<delete id="delete">
		delete from tbl_help_board where bno = #{bno}
	</delete>
	<select id="getAttach" resultType="string">
		select fullName from tbl_help_attach where bno = #{bno} order by regdate
	</select>
	<insert id="addAttach">
		insert into tbl_help_attach(fullName, bno) values (#{fullName}, LAST_INSERT_ID())
	</insert>
	<delete id="deleteAttach">
		delete from tbl_help_attach where bno =#{bno}
	</delete>
	<insert id="replaceAttach">
		insert into tbl_help_attach(fullName, bno) values(#{fullName}, #{bno})
	</insert>
	<delete id="updateViewcnt">
		update tbl_help_board set viewcnt= viewcnt+1 where bno = #{bno}
	</delete>
	<select id="listCriteria" resultType="org.zerock.domain.BoardVO">
		<![CDATA[
		select 
			bno, title, content, writer, regdate, viewcnt, replycnt
			from tbl_help_board 
		where bno > 0 
		order by bno desc, regdate desc
		limit #{pageStart}, #{perPageNum}
		]]>
	</select>
	<select id="countPaging" resultType="int">
		select
			count(bno)
		from
			tbl_help_board
		where
			bno>0
	</select>
	<update id="updateReplyCnt">
		update tbl_help_board set replycnt = replycnt + #{amount} where bno = #{bno}
	</update>
	<select id="listSearch" resultType="org.zerock.domain.BoardVO">
		<![CDATA[
		select *
		from tbl_help_board 
		where bno > 0 
		]]>
		
		<include refid="search"></include>
		
		<![CDATA[
		order by bno desc
		limit #{pageStart}, #{perPageNum}
		]]>
	</select>
	
	<select id="listSearchCount" resultType="int">
		<![CDATA[
		select
			count(bno)
		from tbl_help_board
		where bno > 0
		]]>
		
		<include refid="search"></include>
		
	</select>
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType=='t'.toString()">
				and title like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='c'.toString()">
				and content like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='w'.toString()">
				and writer like CONCAT('%', #{keyword}, '%')
			</if>
			<if test="searchType=='tc'.toString()">
				and (
					title like CONCAT('%', #{keyword}, '%') 
					OR content like CONCAT('%', #{keyword}, '%')
				)
			</if>
			<if test="searchType=='cw'.toString()">
				and (
					content like CONCAT('%', #{keyword}, '%') 
					OR writer like CONCAT('%', #{keyword}, '%')
				)
			</if>
			<if test="searchType=='tcw'.toString()">
				and (
					title like CONCAT('%', #{keyword}, '%') 
					OR content like CONCAT('%', #{keyword}, '%') 
					OR writer like CONCAT('%', #{keyword}, '%')
				)
			</if>
		</if>
	</sql>
	
</mapper>

